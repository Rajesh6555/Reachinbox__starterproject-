/** @type {import('next').NextConfig} */
const nextConfig = {
    // The experimental block is optional; remove it if you don't need it.
    // experimental: {
    //   allowedDevOrigins: ["http://localhost:3000", "http://127.0.0.1:3000"],
    // },
};

export default nextConfig;
